# Copy this file to .env.local (or .env.production for a production build)
# and adjust as needed. Vite only exposes vars prefixed with VITE_.

# Base URL of the api-gateway, including the /api prefix.
# Defaults to http://localhost:8080/api if unset.
VITE_API_BASE_URL=http://localhost:8080/api
